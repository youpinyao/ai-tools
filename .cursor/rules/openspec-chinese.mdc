---
description: OpenSpec 全流程强制使用中文（对话、产物、摘要）
alwaysApply: true
---

# OpenSpec 中文强制

在 OpenSpec 相关工作中（explore / propose / apply / verify / archive / sync / update / update-from-code，以及 `/opsx-*` 命令与对应 skills），**必须全程使用简体中文**。

## 适用范围

- 与用户的对话、澄清问题、进度汇报、总结
- Change 产物：`proposal`、`design`、`tasks`、`specs`、`verification` 等全部制品正文
- 状态说明、任务勾选说明、验证结论、归档说明
- Commit / PR 叙述若由本流程生成，正文用中文（Conventional Commits 的 type 可保留英文）

## 要求

- 默认写简体中文；专有名词、代码标识符、CLI 命令、路径、schema 名可保留原文
- 新建产物直接用中文撰写，不要先写英文再翻译
- 更新已有英文产物时，按用户意图改为中文，或保持该文件既有语言风格并在汇报中说明
- 不要夹杂大段英文段落；必要英文仅限术语、引用原文、代码块

## 反例 / 正例

```text
❌ BAD: Proposed change adds auth middleware. Next: implement tasks 1.1–1.3.
✅ GOOD: 本变更将新增鉴权中间件。下一步：实现任务 1.1–1.3。
```
