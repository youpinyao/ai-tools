## 为什么

<!-- 说明本次变更的动机。它要解决什么问题？为什么是现在？ -->

## 变更内容

<!-- 描述具体变更。明确说明新增能力、修改或移除。 -->

## 能力

### 新增能力
<!-- 本次引入的能力。对新引入的路径段使用 kebab-case
     （例如 user-auth 或 identity/user-auth），并遵循项目既有的
     规范组织方式。每项能力都会创建 specs/<capability-path>/spec.md。 -->
- `<capability-path>`：<简要说明该能力涵盖的内容>

### 修改能力
<!-- 需求发生变化的现有能力（而非只改实现）。
     仅在规范层行为变化时列出。每项能力都需要一个增量规范文件。
     使用 openspec/specs/ 下准确的现有路径。若需求没有变化则留空。
     若完全没有任何能力（纯重构、工具链、文档），必须在 .openspec.yaml
     中设置 `skip_specs: true`——否则 openspec validate 会拒绝零增量变更。
     不要为了通过校验而捏造需求。 -->
- `<existing-capability-path>`：<说明发生变化的需求>

## 影响

<!-- 受影响的代码、API、依赖或系统 -->
